spring.application.name=cdtn
spring.datasource.url=jdbc:postgresql://localhost:5432/cdtn
spring.datasource.username=postgres
spring.datasource.password=270504
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Cấu hình Java Mail Sender
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=longberray88@gmail.com
spring.mail.password=pvgnrpcripolmakb
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
# Optional: display name / from address used for outgoing emails
app.mail.from=CDTN<noreply@yourdomain.com>
app.mail.subject.prefix=[CDTN]
# Whether to log OTP to console when SMTP fails (useful for local/dev). Set to false in production.
app.mail.dev-fallback=true
# Mail send retry config
app.mail.retry.count=3
app.mail.retry.backoffMs=1000

# Task executor for async mail sending
app.mail.executor.corePoolSize=2
app.mail.executor.maxPoolSize=5
app.mail.executor.queueCapacity=50